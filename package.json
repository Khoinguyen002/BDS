{
  "name": "bds-monorepo",
  "private": true,
  "packageManager": "pnpm@10.6.3",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@eslint/compat": "^2.1.0",
    "turbo": "^2.0.0"
  }
}
